package TikiShopMVC.Dao;

import java.util.HashMap;
import java.util.Map;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;
import org.springframework.stereotype.Repository;

import TikiShopMVC.Entity.User;

@Repository
public class UserDao extends BaseDao {
	private NamedParameterJdbcTemplate namedJdbcTemplate; 
	
	final String INSERT_QUERY = "insert into user (user, password, display_name, address) values (:user, :password, :display_name, :address)";
	
	@Autowired
	 public UserDao(NamedParameterJdbcTemplate namedJdbcTemplate){
	    this.namedJdbcTemplate = namedJdbcTemplate;
	 }
	
	public int addAccount(User user){
		int inserted = 0;
		return inserted;
	}

	public int save(User user) {
	    Map<String, Object> paramMap = new HashMap<String, Object>();
	    paramMap.put("user", user.getUser());
	    paramMap.put("password", user.getPassword());
	    paramMap.put("display_name", user.getDisplay_name());
	    paramMap.put("address", user.getAddress());
	    
	    return namedJdbcTemplate.update(INSERT_QUERY, paramMap);
	}

	public void update(User user) {
		// TODO Auto-generated method stub
		
	}

	public void deleteEmpById(long userId) {
		// TODO Auto-generated method stub
		
	} 
}
